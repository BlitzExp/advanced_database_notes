This lesson focuses on schema backup and restore using only SQL access. The main topics are exploring schema objects with USER_OBJECTS, extracting DDL with DBMS_METADATA.GET_DDL, making DDL portable with transform parameters like EMIT_SCHEMA = false, checking dependencies with USER_DEPENDENCIES, planning migration order, and understanding the difference between manual DDL extraction and Oracle Data Pump tools like expdp/impdp. 